version: '3.3'

services:
  bind-dns:
    image: patklaey/bind-dns:latest
    container_name: bind-dns
    restart: always
    network_mode: "host"
    volumes:
      - ./db.patklaey.ch:/etc/bind/zones/db.patklaey.ch
